# Frontend Dashboard Requirements

## NPM Dependencies (see package.json)
# @playwright/test: ^1.54.2

## System Dependencies for Playwright
# Run: sudo apt-get install -y libnspr4 libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libxkbcommon0 libatspi2.0-0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libgbm1 libcairo2 libpango-1.0-0 libasound2

## CDN Dependencies (loaded in index.html)
# Chart.js: 4.x
# Firebase: 10.7.1

## Firebase Services Used
# - Firebase Hosting
# - Firebase Firestore

## Required Firestore Collections
# - PH-dashboard (hot_right_now document)
# - reddit_categories (category documents with time windows)
# - youtube_categories (category documents with subcollections)
# - all_categories (aggregated category data)

## Required Firestore Security Rules
# Read access must be allowed for all above collections